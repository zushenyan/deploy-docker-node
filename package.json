{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "engines": {
    "node": "~8.9.0",
    "yarn": "~1.3.2"
  },
  "scripts": {
    "dev": "nodemon --inspect=0.0.0.0:1337 index.js",
    "start": "node index.js",
    "docker:dev:run": "docker-compose -f docker-compose-dev.yml up -d --build",
    "docker:dev:log": "docker-compose -f docker-compose-dev.yml logs -f",
    "docker:dev:down": "docker-compose -f docker-compose-dev.yml down",
    "docker:dev": "yarn run docker:dev:run && yarn run docker:dev:log"
  },
  "dependencies": {
    "express": "^4.16.2",
    "lodash": "^4.17.5",
    "morgan": "^1.9.0",
    "pg": "^7.4.1",
    "sequelize": "^4.33.3",
    "sequelize-cli": "^3.2.0"
  },
  "devDependencies": {
    "faker": "^4.1.0",
    "nodemon": "^1.14.12"
  }
}
